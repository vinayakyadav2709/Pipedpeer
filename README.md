# Pipedpeer

P2P distributed compute — run Python workloads across your machines with a single command.

This is the **stable** branch. No stable release has been cut yet.

- Active development happens on [`dev`](../../tree/dev) (nightly stream)
- Experimental work lives on [`poc`](../../tree/poc)

Stable releases will land here once the first tagged release passes CI.
